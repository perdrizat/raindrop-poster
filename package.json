{
  "name": "raindrop",
  "version": "1.2.3",
  "main": "index.js",
  "scripts": {
    "dev:client": "pnpm -C client dev",
    "dev:server": "pnpm -C server dev",
    "dev": "concurrently \"pnpm dev:client\" \"pnpm dev:server\"",
    "test": "pnpm -C client test && pnpm -C server test",
    "test:e2e": "pnpm -C server test:e2e",
    "lint": "pnpm -C client lint && pnpm -C server lint",
    "build": "docker build --build-arg BUILD_TIME=\"$(date -u +'%Y-%m-%dT%H:%M:%SZ')\" -t raindrop-poster:$(node -p \"require('./package.json').version\") -t raindrop-poster:latest .",
    "release": "pnpm version patch",
    "deploy:remote": "./scripts/deploy-remote.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "engines": {
    "node": ">=22.13.0 <24.16.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
