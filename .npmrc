# Fail `pnpm install` when the active Node is outside package.json "engines".
# The upper bound (<24.16.0) blocks the Node 24.16/24.17 regression that silently
# breaks puppeteer's Chrome extraction (nodejs/node#63487). See CONTRIBUTING.md.
engine-strict=true
